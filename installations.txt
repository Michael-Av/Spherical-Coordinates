sudo apt install python3-pip
export PATH=$PATH:~/.local/bin // should go in .bashrc
// May need to create a virtual environment to allow pip installations
pip install timezonefinder
pip install pytz
pip install numpy
pip install matplotlib
pip install pyvista
